document.addEventListener('DOMContentLoaded', function () {
    const bodyEl = document.body;
    const header = document.getElementById('header');
    const themeToggle = document.getElementById('themeToggle');
    const mobileMenuBtn = document.getElementById('mobileMenuBtn');
    const navLinks = document.getElementById('navLinks');
    const menuOverlay = document.getElementById('menuOverlay');


    // Dark / light toggle button
    const prefersDark = window.matchMedia('(prefers-color-scheme: dark)');
    const savedTheme = localStorage.getItem('theme');
    const sunIcon = document.querySelector('.fa-sun');
    const moonIcon = document.querySelector('.fa-moon');

    if (savedTheme === 'dark' || (!savedTheme && prefersDark.matches)) {
        bodyEl.classList.add('dark-mode');
        sunIcon.style.display = 'none';
        moonIcon.style.display = 'inline-block';
    } else {
        sunIcon.style.display = 'inline-block';
        moonIcon.style.display = 'none';
    }

    if (themeToggle) {
        themeToggle.addEventListener('click', function () {
            bodyEl.classList.toggle('dark-mode');

            if (bodyEl.classList.contains('dark-mode')) {
                localStorage.setItem('theme', 'dark');
                sunIcon.style.display = 'none';
                moonIcon.style.display = 'inline-block';
            } else {
                localStorage.setItem('theme', 'light');
                sunIcon.style.display = 'inline-block';
                moonIcon.style.display = 'none';
            }
        });
    }

    prefersDark.addEventListener('change', function (e) {
        if (!localStorage.getItem('theme')) {
            if (e.matches) {
                bodyEl.classList.add('dark-mode');
                sunIcon.style.display = 'none';
                moonIcon.style.display = 'inline-block';
            } else {
                bodyEl.classList.remove('dark-mode');
                sunIcon.style.display = 'inline-block';
                moonIcon.style.display = 'none';
            }
        }
    });



    // MOBILE MENU
    function openMenu() {
        navLinks.classList.add('active');
        menuOverlay.classList.add('active');
        bodyEl.classList.add('menu-open');
        mobileMenuBtn.innerHTML = '<i class="fas fa-times"></i>';
    }

    function closeMenu() {
        navLinks.classList.remove('active');
        menuOverlay.classList.remove('active');
        bodyEl.classList.remove('menu-open');
        mobileMenuBtn.innerHTML = '<i class="fas fa-bars"></i>';
    }

    if (mobileMenuBtn) {
        mobileMenuBtn.addEventListener('click', () => {
            navLinks.classList.contains('active') ? closeMenu() : openMenu();
        });
    }

    if (menuOverlay) {
        menuOverlay.addEventListener('click', closeMenu);
    }

    if (navLinks) {
        navLinks.querySelectorAll('a').forEach(link => {
            link.addEventListener('click', () => {
                closeMenu();
                navLinks.querySelectorAll('a').forEach(a => a.classList.remove('active'));
                link.classList.add('active');
            });
        });
    }


    // SMOOTH SCROLLING
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener('click', function (e) {
            const targetId = this.getAttribute('href');
            if (targetId === '#') return;
            const targetEl = document.querySelector(targetId);
            if (targetEl) {
                e.preventDefault();
                closeMenu();
                const headerHeight = header?.offsetHeight || 75;
                const top = targetEl.getBoundingClientRect().top + window.scrollY - headerHeight - 10;
                window.scrollTo({ top, behavior: 'smooth' });
            }
        });
    });


    // ACTIVE NAV LINK ON SCROLL
    const sections = document.querySelectorAll('section[id]');
    const navItems = document.querySelectorAll('.nav-links a');

    function onScroll() {
        const scrollY = window.scrollY;
        let current = '';

        sections.forEach(section => {
            if (scrollY >= section.offsetTop - 120) {
                current = section.getAttribute('id');
            }
        });

        navItems.forEach(item => {
            item.classList.remove('active');
            if (current && item.getAttribute('href') === `#${current}`) {
                item.classList.add('active');
            }
        });

        if (header) {
            header.style.boxShadow = scrollY > 50
                ? '0 4px 25px rgba(0,0,0,0.12)'
                : '0 2px 20px rgba(0,0,0,0.08)';
        }
    }

    window.addEventListener('scroll', onScroll, { passive: true });
    onScroll();



    // SKILL ORBITS ANIMATION
    const skills = [
        { icon: 'fab fa-html5', name: 'HTML5' },
        { icon: 'fab fa-css3-alt', name: 'CSS3' },
        { icon: 'fab fa-js-square', name: 'JavaScript' },
        { icon: 'fab fa-php', name: 'PHP' },
        { icon: 'fas fa-database', name: 'MySQL' },
        { icon: 'fab fa-bootstrap', name: 'Bootstrap' },
        { icon: 'fab fa-linkedin', name: 'LinkedIn' },
        { icon: 'fab fa-figma', name: 'Figma' },
        { icon: 'fab fa-git-alt', name: 'Git' },
        { icon: 'fab fa-wordpress', name: 'WordPress' },
        { icon: 'fab fa-google', name: 'Google Ads' },
        { icon: 'fab fa-facebook', name: 'Meta Ads' },
        { icon: 'fas fa-search', name: 'SEO' },
        { icon: 'fas fa-chart-pie', name: 'Analytics' },
        { icon: 'fab fa-youtube', name: 'Youtube' },
        { icon: 'fas fa-paint-brush', name: 'Canva' },
        { icon: 'fas fa-code', name: 'VS Code' },
        { icon: 'fas fa-mobile-alt', name: 'Responsive' },
        { icon: 'fas fa-envelope-open-text', name: 'Email' },
        { icon: 'fas fa-hashtag', name: 'Social Media' }
    ];

    const orbitsConfig = [
        { className: 'skill-orbit skill-orbit-1', count: 6 },
        { className: 'skill-orbit skill-orbit-2', count: 7 },
        { className: 'skill-orbit skill-orbit-3', count: 4 },
        { className: 'skill-orbit skill-orbit-4', count: 3 }
    ];

    function createSkillsOrbits() {
        const orbitsContainer = document.getElementById('skillsOrbits');
        if (!orbitsContainer) return;

        orbitsContainer.innerHTML = '';
        let skillIndex = 0;

        orbitsConfig.forEach((orbit, orbitIndex) => {
            const orbitEl = document.createElement('div');
            orbitEl.className = orbit.className;

            for (let i = 0; i < orbit.count && skillIndex < skills.length; i++) {
                const skill = skills[skillIndex];
                const angle = (i / orbit.count) * 2 * Math.PI;
                const radius = 50 + (orbitIndex * 25);

                const badge = document.createElement('div');
                badge.className = 'skill-badge';
                badge.innerHTML = `<i class="${skill.icon}"></i>`;
                badge.setAttribute('data-name', skill.name);
                badge.style.left = `${50 + radius * Math.cos(angle)}%`;
                badge.style.top = `${50 + radius * Math.sin(angle)}%`;
                badge.style.transform = 'translate(-50%, -50%)';

                badge.addEventListener('mouseenter', function () {
                    const tooltip = document.createElement('div');
                    tooltip.className = 'skill-tooltip';
                    tooltip.textContent = this.getAttribute('data-name');
                    Object.assign(tooltip.style, {
                        position: 'absolute',
                        bottom: '120%',
                        left: '50%',
                        transform: 'translateX(-50%)',
                        background: bodyEl.classList.contains('dark-mode') ? 'var(--dark)' : 'var(--primary)',
                        color: 'white',
                        padding: '5px 10px',
                        borderRadius: '5px',
                        fontSize: '0.8rem',
                        whiteSpace: 'nowrap',
                        zIndex: '1000',
                        pointerEvents: 'none'
                    });
                    this.appendChild(tooltip);
                });

                badge.addEventListener('mouseleave', function () {
                    const tooltip = this.querySelector('.skill-tooltip');
                    if (tooltip) tooltip.remove();
                });

                orbitEl.appendChild(badge);
                skillIndex++;
            }

            orbitsContainer.appendChild(orbitEl);
        });
    }

    createSkillsOrbits();

    let resizeTimeout;
    window.addEventListener('resize', () => {
        clearTimeout(resizeTimeout);
        resizeTimeout = setTimeout(createSkillsOrbits, 350);
    });



    // Scroll animation (observer re-runs after projects render)
    let statsAnimated = false;

    function observeAnimatedElements() {
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                    if (entry.target.id === 'stats' && !statsAnimated) {
                        statsAnimated = true;
                        animateCounters();
                    }
                }
            });
        }, { threshold: 0.1, rootMargin: '0px 0px -60px 0px' });

        document.querySelectorAll('.skill-card, .project-card, .journey-card, .stat-item').forEach(el => {
            observer.observe(el);
        });

        const statsSection = document.getElementById('stats');
        if (statsSection) observer.observe(statsSection);

        setTimeout(() => {
            document.querySelectorAll('.skill-card, .project-card, .journey-card, .stat-item').forEach(el => {
                const rect = el.getBoundingClientRect();
                if (rect.top < window.innerHeight && rect.bottom > 0) {
                    el.classList.add('visible');
                }
            });
        }, 100);
    }

    // Stat conunter
    function animateCounters() {
        const counters = document.querySelectorAll('.stat-number');
        const duration = 1500;

        counters.forEach(counter => {
            const target = parseInt(counter.getAttribute('data-count'), 10) || 0;
            if (target === 0) return;

            const startTime = performance.now();

            function update(currentTime) {
                const progress = Math.min((currentTime - startTime) / duration, 1);
                const eased = 1 - Math.pow(1 - progress, 3);
                counter.innerText = Math.round(eased * target);
                if (progress < 1) requestAnimationFrame(update);
                else counter.innerText = target;
            }

            requestAnimationFrame(update);
        });
    }

    // Project section data
    const projectsList = [
        {
            category: "web-design",
            categoryLabel: "Web Design",
            image: "assets/thumbnail Architect.jpg",
            imageAlt: "Architect Website preview by Azmat Ali",
            title: "Responsive Architectural Website",
         //   description: "A modern architectural website with smooth animations and full responsive design. Created for a client portfolio.",
            description: Some task is pending...
            projectLink: "https://xrylq4.csb.app/#home",
            linkIcon: "fas fa-external-link-alt",
            buttonText: "Visit Site",
            technologies: ["HTML", "CSS", "JavaScript", "Apps Script", "Responsive"]
        },
        {
            category: "web-development",
            categoryLabel: "Web Development",
            image: "assets/thumbnail movieflix.jpg",
            imageAlt: "Movieflix clone preview",
            title: "Movieflix",
            description: "My first complete project! A responsive movie catalog website similar to Netflix, with movie details and smooth layout.",
            projectLink: "https://movieeeflix.vercel.app/",
            linkIcon: "fas fa-external-link-alt",
            buttonText: "View Site",
            technologies: ["HTML", "CSS", "JavaScript"]
        },
        {
            category: "web-development",
            categoryLabel: "Web Development",
            image: "assets/thumbnail IQ tester.jpg",
            imageAlt: "IQ Tester Q&A app",
            title: "IQ Tester Q&A",
            description: "Interactive quiz application with user authentication, question database and score tracking. Built with PHP and MySQL.",
            projectLink: "https://github.com/azmataliofficial/IQ-Tester-Q-A",
            linkIcon: "fab fa-github",
            buttonText: "View Code",
            technologies: ["HTML", "CSS", "JS", "PHP", "MySQL"]
        },
        {
            category: "web-design",
            categoryLabel: "Web Design",
            image: "assets/thumbnail car.jpg",
            imageAlt: "Car website design",
            title: "Car's Website",
            description: "Modern car showcase website with interactive elements. One of my beginner projects that focused on clean UI.",
            projectLink: "https://car-comparison-pi.vercel.app/",
            linkIcon: "fas fa-external-link-alt",
            buttonText: "Visit Site",
            technologies: ["HTML", "CSS"]
        },
        {
            category: "web-design",
            categoryLabel: "Web Design",
            image: "assets/thumbnail tolet.jpg",
            imageAlt: "To-let hotel rooms",
            title: "To-let Hotel Rooms",
            description: "Elegant hotel room listing website. Fully responsive design, perfect for beginners learning layouts.",
            projectLink: "https://to-let-hotel-rooms.vercel.app/",
            linkIcon: "fas fa-external-link-alt",
            buttonText: "Visit Site",
            technologies: ["HTML", "CSS", "Bootstrap", "Responsive"]
        },
        {
            category: "web-development",
            categoryLabel: "Web Development",
            image: "assets/thumbnail oxford.jpg",
            imageAlt: "Oxford University clone",
            title: "Oxford Clone",
            description: "Educational website clone showing college details and image gallery. Built as a practice project.",
            projectLink: "https://project-oxford-website.vercel.app/",
            linkIcon: "fas fa-external-link-alt",
            buttonText: "View Site",
            technologies: ["HTML", "CSS", "Beginner Friendly"]
        },
        {
            category: "digital-marketing",
            categoryLabel: "Digital Marketing",
            image: "assets/thumbnail digital marketing.webp",
            imageAlt: "Marketing dashboard",
            title: "Marketing Analytics Dashboard",
            description: "Coming soon.............",
            projectLink: "#",
            linkIcon: "fas fa-chart-line",
            buttonText: "View Details",
            technologies: ["HTML/CSS", "JavaScript", "Bootstrap", "API"]
        }
    ];

    function buildProjectCard(project) {
        const techHtml = project.technologies.map(t => `<span class="tech-tag">${t}</span>`).join('');
        return `
        <div class="project-card">
            <div class="project-img-container">
                <img src="${project.image}" alt="${project.imageAlt}" class="project-img" loading="lazy">
                <div class="project-overlay">
                    <a href="${project.projectLink}" class="btn" target="_blank">
                        <i class="${project.linkIcon}"></i> ${project.buttonText}
                    </a>
                </div>
            </div>
            <div class="project-content">
                <span class="project-category">${project.categoryLabel}</span>
                <h3>${project.title}</h3>
                <p>${project.description}</p>
                <div class="project-tech">${techHtml}</div>
            </div>
        </div>`;
    }

    function renderProjects(filterValue) {
        const grid = document.getElementById('projectsGrid');
        const filtered = filterValue === 'all'
            ? projectsList
            : projectsList.filter(p => p.category === filterValue);

        if (filtered.length > 0) {
            grid.innerHTML = filtered.map(buildProjectCard).join('');
        } else {
            grid.innerHTML = `<div class="no-projects">No projects found in this category.</div>`;
        }

        observeAnimatedElements();
    }

    // Tab filter buttons
    document.querySelectorAll('.tab-btn').forEach(button => {
        button.addEventListener('click', function () {
            document.querySelectorAll('.tab-btn').forEach(btn => btn.classList.remove('active'));
            this.classList.add('active');
            renderProjects(this.getAttribute('data-tab'));
        });
    });

    renderProjects('all');
    observeAnimatedElements();


    // Testimonial slide section
    const testimonials = [
        {
            text: "I was impressed with the dedication and willingness to learn. You quickly adapted to our workflow and delivered quality work within tight deadlines. Great potential!",
            name: "Sanjay Upadhya",
            role: "Senior Trainer, Tech Access",
            avatar: "https://cdn-icons-png.flaticon.com/512/18269/18269822.png"
        },
        {
            text: "Your understanding of modern web technologies as a fresher is impressive. You implemented complex features with minimal guidance. Keep up the good work!",
            name: "Mohd Aftab",
            role: "Social Media Manager, Digixfly",
            avatar: "https://cdn-icons-png.flaticon.com/512/18269/18269822.png"
        },
        {
            text: "Your design for our studio website was fresh and innovative. As a fresher, you showed great creativity and technical skill. The responsive design works perfectly across all devices.",
            name: "Arif Saifi",
            role: "Architect & Founder, Arc Square Design Studio",
            avatar: "https://cdn-icons-png.flaticon.com/512/18269/18269822.png"
        }
    ];

    const sliderTrack = document.getElementById('testimonialSlider');
    const timerIndicators = document.getElementById('timerIndicators');

    if (sliderTrack && timerIndicators) {
        let currentSlide = 0;
        let slideTimer;

        testimonials.forEach((t, i) => {
            sliderTrack.innerHTML += `
            <div class="testimonial-slide">
                <div class="quote-icon"><i class="fas fa-quote-left"></i></div>
                <p class="testimonial-text">${t.text}</p>
                <div class="client-info">
                    <div class="client-avatar">
                        <img src="${t.avatar}" alt="${t.name}" loading="lazy">
                    </div>
                    <h3 class="client-name">${t.name}</h3>
                    <p class="client-role">${t.role}</p>
                </div>
            </div>`;

            const dot = document.createElement('div');
            dot.className = `timer-dot${i === 0 ? ' active' : ''}`;
            dot.dataset.slide = i;
            timerIndicators.appendChild(dot);
        });

        function goToSlide(n) {
            currentSlide = n;
            sliderTrack.style.transform = `translateX(-${n * 100}%)`;
            timerIndicators.querySelectorAll('.timer-dot').forEach((dot, i) => {
                dot.classList.toggle('active', i === n);
            });
        }

        function startAuto() {
            slideTimer = setInterval(() => {
                goToSlide((currentSlide + 1) % testimonials.length);
            }, 4500);
        }

        timerIndicators.querySelectorAll('.timer-dot').forEach((dot, i) => {
            dot.addEventListener('click', () => {
                clearInterval(slideTimer);
                goToSlide(i);
                startAuto();
            });
        });

        startAuto();
    }

    // Contact form
    const contactForm = document.getElementById('contactForm');

    if (contactForm) {
        contactForm.addEventListener('submit', async function (e) {
            e.preventDefault();

            const submitBtn = document.getElementById('cf-btn');
            const originalBtnText = submitBtn.innerHTML;

            submitBtn.disabled = true;
            submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Sending...';

            const formData = {
                name: document.getElementById('cf-name').value.trim(),
                email: document.getElementById('cf-email').value.trim(),
                phone: document.getElementById('cf-phone').value.trim(),
                subject: document.getElementById('cf-subject').value.trim(),
                message: document.getElementById('cf-message').value.trim()
            };

            const SCRIPT_URL = 'https://script.google.com/macros/s/AKfycbxZf_k5H-TlClZ3LtLjikc5u2u2Q1RgaKHQ1X1pWokFwqUP8NaMUlAN3QVFEc6w_Y-B/exec';
            try {
                await fetch(SCRIPT_URL, {
                    method: 'POST',
                    mode: 'no-cors',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(formData)
                });

                createSuccessAnimation();
                this.reset();

            } catch (error) {
                console.error('Error:', error);
                alert('There was an error sending your message. Please try again later.');
            } finally {
                submitBtn.disabled = false;
                submitBtn.innerHTML = originalBtnText;
            }
        });
    }

    // Success animation after submit contact form
    function createSuccessAnimation() {
        const el = document.createElement('div');
        el.innerHTML = '<i class="fas fa-check-circle"></i>';
        Object.assign(el.style, {
            position: 'fixed',
            top: '50%',
            left: '50%',
            transform: 'translate(-50%, -50%) scale(0)',
            fontSize: '90px',
            color: '#10b981',
            zIndex: '9999',
            pointerEvents: 'none'
        });
        document.body.appendChild(el);

        const anim = el.animate([
            { transform: 'translate(-50%, -50%) scale(0)', opacity: 0 },
            { transform: 'translate(-50%, -50%) scale(1.2)', opacity: 1 },
            { transform: 'translate(-50%, -50%) scale(1)', opacity: 1 },
            { transform: 'translate(-50%, -50%) scale(0)', opacity: 0 }
        ], { duration: 2000, easing: 'cubic-bezier(0.175, 0.885, 0.32, 1.275)' });

        anim.onfinish = () => el.remove();
    }

}); // End DOMContentLoaded